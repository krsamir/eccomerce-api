✅ Prerequisites

Before you begin, ensure you have the following:

1. Yarn installed globally
   This project uses Yarn Workspaces, so make sure Yarn is installed on your machine.
   You can install it globally using:

`npm install -g yarn`

2. Environment variables
   Create a .env file in the root directory.

📦 Install Dependencies

To install all required packages, run the following command in the project root:

`yarn`
